# Contributing

Contributions should preserve the protocol boundary:

- no client data;
- no private engine source;
- no unverifiable performance claims;
- no external-certification language unless the evidence is actually attached;
- no serialized numeric `value` when a metric status is `WITHHELD`.

Pull requests should include validator tests for any schema or protocol behavior change.
